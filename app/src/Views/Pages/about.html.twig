{% extends 'base.html.twig' %}

{% block main %}
    <div style="margin: 0 auto; width: fit-content;">
        <h2>About</h2>
        {% if content %}
            <p>{{ content }}</p>
        {% endif %}
    </div>
{% endblock %}